name: Test Workflow Dispatch

on:
  workflow_dispatch:
    inputs:
      test_input:
        description: "Test input"
        required: true
        type: string

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "Test input: ${{ github.event.inputs.test_input }}"'
        shell: bash
