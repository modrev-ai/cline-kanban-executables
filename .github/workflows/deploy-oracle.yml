name: Deploy to Oracle Compute

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        type: choice
        options:
          - production
        default: 'production'
      force:
        description: 'Force deployment even if no changes'
        type: boolean
        default: false
        required: false

jobs:
  deploy:
    name: Deploy to Oracle Compute
    runs-on: windows-latest
    environment: ${{ github.event.inputs.environment }}
    timeout-minutes: 30

    env:
      ORACLE_HOST: ${{ secrets.ORACLE_HOST }}
      ORACLE_USER: ${{ secrets.ORACLE_USER }}
      ORACLE_SSH_KEY: ${{ secrets.ORACLE_SSH_KEY }}
      DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup SSH key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ env.ORACLE_SSH_KEY }}

      - name: Add Oracle host to known hosts
        run: |
          ssh-keyscan -H ${{ env.ORACLE_HOST }} >> $env:USERPROFILE\.ssh\known_hosts

      - name: Test SSH connection
        run: |
          ssh -o ConnectTimeout=10 -o BatchMode=yes ${{ env.ORACLE_USER }}@${{ env.ORACLE_HOST }} "echo 'SSH connection successful'"

      - name: Create deploy directory on Oracle
        run: |
          ssh ${{ env.ORACLE_USER }}@${{ env.ORACLE_HOST }} "mkdir -p ${{ env.DEPLOY_PATH }}/prod_executable"

      - name: Deploy prod_executable files via SCP
        run: |
          scp -r prod_executable/* ${{ env.ORACLE_USER }}@${{ env.ORACLE_HOST }}:${{ env.DEPLOY_PATH }}/prod_executable/

      - name: Deploy start scripts via SCP
        run: |
          scp start-all.bat start-all.ps1 start-all-with-tailscale.bat start-all-with-tailscale.ps1 ${{ env.ORACLE_USER }}@${{ env.ORACLE_HOST }}:${{ env.DEPLOY_PATH }}/

      - name: Deploy README
        run: |
          scp README.md ${{ env.ORACLE_USER }}@${{ env.ORACLE_HOST }}:${{ env.DEPLOY_PATH }}/

      - name: Create systemd service files on Oracle
        run: |
          ssh ${{ env.ORACLE_USER }}@${{ env.ORACLE_HOST }} "cat > /etc/systemd/system/kanban-proxy.service << 'EOF'
[Unit]
Description=Kanban Header Rewriting Proxy
After=network.target

[Service]
Type=simple
User=opc
WorkingDirectory=${{ env.DEPLOY_PATH }}/prod_executable
Environment=NODE_PATH=/home/opc/.npm-global/lib/node_modules
ExecStart=/usr/bin/node kanban-proxy.js
Restart=on-failure
RestartSec=5
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
EOF"

      - name: Create kanban-server systemd service
        run: |
          ssh ${{ env.ORACLE_USER }}@${{ env.ORACLE_HOST }} "cat > /etc/systemd/system/kanban-server.service << 'EOF'
[Unit]
Description=Kanban Server (Cline)
After=network.target

[Service]
Type=simple
User=opc
WorkingDirectory=${{ env.DEPLOY_PATH }}
Environment=KANBAN_RUNTIME_HOST=127.0.0.1
Environment=KANBAN_RUNTIME_PORT=3485
Environment=NODE_ENV=production
ExecStart=/usr/bin/npx cline kanban
Restart=on-failure
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
EOF"

      - name: Reload systemd and enable services
        run: |
          ssh ${{ env.ORACLE_USER }}@${{ env.ORACLE_HOST }} "
            sudo systemctl daemon-reload &&
            sudo systemctl enable kanban-proxy kanban-server &&
            sudo systemctl restart kanban-proxy kanban-server
          "

      - name: Wait for services to start
        run: |
          ssh ${{ env.ORACLE_USER }}@${{ env.ORACLE_HOST }} "
            sleep 10 &&
            systemctl status kanban-proxy --no-pager &&
            systemctl status kanban-server --no-pager
          "

      - name: Check service status
        run: |
          ssh ${{ env.ORACLE_USER }}@${{ env.ORACLE_HOST }} "
            echo '=== kanban-proxy status ===' &&
            systemctl status kanban-proxy --no-pager -l &&
            echo '' &&
            echo '=== kanban-server status ===' &&
            systemctl status kanban-server --no-pager -l &&
            echo '' &&
            echo '=== Port status ===' &&
            ss -tlnp | grep -E ':3484|:3485' &&
            echo '' &&
            echo '=== Tailscale status ===' &&
            tailscale status 2>/dev/null || echo 'Tailscale not running'
          "

      - name: Deployment summary
        run: |
          echo "## Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Property | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Environment | ${{ github.event.inputs.environment }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Oracle Host | ${{ env.ORACLE_HOST }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Deploy Path | ${{ env.DEPLOY_PATH }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Deployed At | $(date -u) |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Services Deployed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- kanban-proxy.service (port 3484 - header rewriting proxy)" >> $GITHUB_STEP_SUMMARY
          echo "- kanban-server.service (port 3485 - Kanban app)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Access" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Kanban Proxy (Tailscale)**: http://$(ssh ${{ env.ORACLE_USER }}@${{ env.ORACLE_HOST }} 'tailscale ip -4'):3484" >> $GITHUB_STEP_SUMMARY
          echo "- **Kanban Server (Direct)**: http://${{ env.ORACLE_HOST }}:3485" >> $GITHUB_STEP_SUMMARY
